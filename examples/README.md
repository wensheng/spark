# Spark Examples

Some examples require additional Python packages, install with:

     pip install -r requirements.txt

## Running examples

Change to examples directory with `cd examples`.

You can then run examples directly:

    python e001_hello.py

